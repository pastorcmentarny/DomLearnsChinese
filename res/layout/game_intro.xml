<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/default_screen_scroll_view">

    <LinearLayout style="@style/default_screen_layout">

        <TextView
            android:id="@+id/intro_title"
            style="@style/title"
            android:text="@string/unknown" />

        <TextView
            android:id="@+id/intro_about"
            style="@style/about"
            android:text="@string/unknown" />

        <Button
            android:id="@+id/play_game_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/play_game" />

        <TextView
            android:id="@+id/intro_tutorial"
            style="@style/tutorial"
            android:text="@string/unknown" />

        <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:ads="http://schemas.android.com/apk/res-auto"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:gravity="center|bottom|center_horizontal"
            android:orientation="vertical">

            <com.google.android.gms.ads.AdView
                android:id="@+id/adView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                ads:adSize="BANNER"
                ads:adUnitId="@string/adUnitId" />
        </LinearLayout>
    </LinearLayout>

</ScrollView>